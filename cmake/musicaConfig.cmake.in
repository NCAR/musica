@PACKAGE_INIT@

# include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@_Exports.cmake")

# check_required_components("@PROJECT_NAME@")

# set(_supported_components musica yaml-cpp micm tuvx_object)

# foreach(_comp IN LISTS ${CMAKE_FIND_PACKAGE_NAME}_FIND_COMPONENTS)
#   if (NOT _comp IN_LIST _supported_components)
#     set(${CMAKE_FIND_PACKAGE_NAME}_FOUND FALSE)
#     set(${CMAKE_FIND_PACKAGE_NAME}_${_comp}_FOUND FALSE)
#     message(FATAL_ERROR "Unsupported component: ${_comp}")
#   endif()
# endforeach()

include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@_Exports.cmake")

check_required_components("@PROJECT_NAME@")