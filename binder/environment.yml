name: musica-env
channels:
  - conda-forge
dependencies:
  - python>=3.9
  - ipykernel
  - ipycytoscape
  - pip
  # Build dependencies for compiling the C++ extension
  - cmake
  - cxx-compiler
  - c-compiler
  - libnetcdf
  - lapack
